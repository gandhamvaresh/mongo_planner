<%= form_for(@plan) do |f| %>
  <% if @plan.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@plan.errors.count, "error") %> prohibited
        this plan from being saved:</h2>
    <ul>
    <% @plan.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
 
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  
  <div class="field">
    <%= f.label :plan_detail %><br />
    <%= f.text_area :plan_detail %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
